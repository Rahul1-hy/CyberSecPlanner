version: '3.8'

services:
  cybersec-planner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cybersec_planner_app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
